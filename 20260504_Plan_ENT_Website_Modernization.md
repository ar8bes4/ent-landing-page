---
title: "実装チェックリスト：ENT Website Modernization（WixからTailwind/Headless移行）"
created: 2026-05-04
tags:
  - Antigravity/Plan
  - project/web-modernization
  - architecture
  - Jamstack
  - hospital
  - Wix
  - Cloudflare-pages
  - GAS
status: "Active"
---

# Project Architecture & Context Document: ENT Website Modernization

## 1. プロジェクトの背景と目的
本プロジェクトは、大学病院（耳鼻咽喉科など）のウェブサイトの一部のリニューアルおよびモダン化を目的とする。
現在、サイトはWix（プレミアムプラン：スモールビジネス）で運用されているが、以下の課題（WixのGUIによる制約）を解決するため、**「完全なコードベース（Vibe Coding）での開発」と「Headlessアーキテクチャ」への移行**を行う。

### 想定読者の考え方
本サイトは単一の読者層に固定しない。ページの種類によって、以下のように読者が変わる前提で設計する。

*   **医局関連ページ:** 現在医局に所属している耳鼻咽喉科医、他大学の医局に所属する耳鼻咽喉科医
*   **一般向けページ:** 市民、患者さん、ご家族
*   **教育・採用ページ:** 医学生、研修医、若手医師

どのページでも、専門性は維持しつつ、必要に応じて用語の補足や背景説明を入れて、読み手に合わせてわかりやすく書く。

### 解決したい課題（Wixの限界）
*   **GUI税（タイムロス）：** AI（Antigravity等）でロジックを一瞬で生成できても、Wixエディタ上でリピーターやテキストボックスを手作業で配置・紐付けする作業が開発体験（Vibe Coding）を著しく阻害している。
*   **ブラックボックス化：** Wix CMSの隠蔽されたフィールドIDや独自のAPI作法（Veloのライフサイクル等）により、柔軟なデータ操作（複数条件ソート等）に無駄な労力がかかる。
*   **バージョン管理の不在：** コードベースでの差分管理（Git）ができないため、破壊的変更に対する心理的ハードルが高い。
*   **Vibe Coding の実践：** VS Code の Codex エクステンションを活用し、AI との対話形式でロジックとデザインを即座に反映させる「Vibe Coding」を本プロジェクトの標準開発手法とする。

## 2. 技術スタック（Target Architecture）
Wixの独自機能・インフラへの依存を脱却し、以下のJamstack的なモダンアーキテクチャを採用する。

| 役割               | 採用技術                                           | 選定理由                                                                                                               |
| :--------------- | :--------------------------------------------- | :----------------------------------------------------------------------------------------------------------------- |
| **フロントエンド**      | **HTML + CSS (Tailwind CSS 等) + Vanilla JS**   | AIによるコード生成と最も相性が良く、WixのCustom Elements（Web Components）としても流用可能。                                                    |
| **ホスティング**       | **Cloudflare Pages**                           | 商用利用（病院サイト）が完全無料。Gemini APIなどを叩くバックエンド処理（Cloudflare Functions）が実装可能。通信量（帯域）が無制限。Git連携による自動デプロイとロールバック（1クリック復元）が可能。 |
| **データベース / API** | **Google スプレッドシート + Google Apps Script (GAS)** | スタッフリストなどの更新を非エンジニア（医療スタッフ）でも簡単に行えるようにするため. GASをJSON API（`doGet`）として構築し、フロントからFetchする。                             |
| **画像・ギャラリー管理**   | **Instagram Embed (埋め込み)**                     | 大量の写真公開によるリポジトリ容量の圧迫を防ぐ。スマホからの手軽な更新を実現。                                                                            |
| **フォーム（お問い合わせ）** | **Google Forms (iframe埋め込み)**                  | 自前でのスパム対策やバックエンド実装のコストをゼロにするため。                                                                                    |
| **バージョン管理**      | **GitHub**                                     | 履歴管理。変更差分の可視化。                                                                                                     |

### デザイン・表現の前提
`rules.md` に従い、以下を標準とする。

*   Tailwind CSS は使うが、デフォルトの `blue-500` などは使わず、ブランド色を優先する
*   日本語は原則として敬体で記述する
*   日本語本文は `Noto Sans JP`、英数字は `Inter` を前提にする
*   セクションやカードの出現演出は `.reveal` と `IntersectionObserver` を基本にする
*   医療用語は、読者層に応じて必要な補足を付ける

## 3. 開発フェーズと移行戦略
現在Wixの有料プランを契約中のため、無駄なコストをかけず、安全に移行する。

*   **Phase 1: 開発とCloudflare Pagesでの検証（Now）**
    *   Wixを稼働させたまま、Antigravity上で全く新しいUI（Tailwind等）を開発する。
    *   GAS APIとの連携、Instagram埋め込み、Google Form埋め込みを実装。
    *   Cloudflare Pages（`.pages.dev`）にデプロイし、本番同等の動作確認を行う。
*   **Phase 2: Wixへの部分適用（オプション）**
    *   必要に応じて、完成したHTML/JSをWixの「カスタム要素」として埋め込み、段階的にWix内のGUIコンポーネントをコードに置き換える。
    *   ※完全に脱Wixの目処が立てばこのステップはスキップ可能。
*   **Phase 3: 完全移行（Wix契約更新前）**
    *   独自ドメインのDNS（Aレコード/CNAME）をWixからCloudflare Pagesに変更。
    *   Wixを解約。ランニングコストをほぼゼロ（ドメイン維持費のみ）にする。

## 4. Wixへの統合戦略（Site Embedding）
Wixを単なる「ホスティング用の箱（シェル）」として利用し、中身をCloudflare Pagesで構築する。この際、最も効率的でVibe Codingに適した手法として**「サイトの埋め込み（iframe）」**を採用する。

### 採用理由
*   **デザインの隔離:** Tailwind CSSなどの外部フレームワークがWix側のスタイル干渉を受けず、独立して動作できる。
*   **開発効率:** AIに対して「1枚の独立したHTMLページ」として作成を指示できるため、プロンプトがシンプルになる。
*   **デプロイフロー:** GitHubへのPushで自動更新され、Wixエディタを開く必要がない。

### 具体的な作業の流れ
1.  **Antigravityで開発:** 目的の機能（スタッフ一覧等）を1枚のHTML/CSS/JSとして完成させる。
2.  **Cloudflare Pagesへ公開:** GitHub経由でデプロイし、公開URLを取得。
3.  **Wixで「サイトの埋め込み」を配置:** パーツ追加メニューから「サイトの埋め込み」を選択。
4.  **URLを設定:** 公開URLを入力。
5.  **枠の調整:** パーツをページいっぱいに広げ、Wixのヘッダー・フッターと馴染ませる。

## 5. Antigravityへの指示・制約事項（Directives for AI）
このコンテキストを読み込んだAIアシスタント（あなた）は、今後の開発において以下のルールを厳守すること。

1.  **GUI前提の提案をしない:** Wixの操作手順や、ノーコードツールの利用は提案しない。すべて「コード（HTML/CSS/JS/Python等）」で解決するアプローチをとること。
2.  **Vibe Codingに最適化する:** 見た目のスタイリング（Tailwind CSS推奨）や、APIフェッチのロジックは、ユーザーがコピペ一発で動作するように完全なファイルとして出力すること。
3.  **GAS連携を前提とする:** 動的データ（スタッフ一覧、お知らせ等）は、ハードコードせず、外部のJSON APIから取得してDOMを展開する前提でJavaScriptを書くこと。
4.  **iframe内動作を前提とする:** 出力するHTMLは、Wixのiframe内で表示されることを考慮し、1枚で完結した、かつレスポンシブな構造にすること。
5.  **ブランド制約を守る:** `rules.md` のブランド色、フォント、アニメーション、文体の制約を前提に設計すること。
6.  **読者層を固定しない:** ページごとに想定読者が異なるため、医局向け・一般向け・採用教育向けのいずれにも対応できるよう、文体と説明密度を切り替えること。
7.  **セキュリティを考慮する:** Gemini APIなどのシークレットキーを使用する機能を追加する際は、フロントエンドにキーを露出させず、Cloudflare Functionsなどのサーバーレス環境を経由するアーキテクチャを提案すること。

## 6. 実装チェックリスト
この章は、実装時にそのまま確認できる作業チェックリストである。  
上から順に進めることを基本とし、完了した項目はチェックを入れる。

### 6-1. 事前確認
- [ ] `rules.md` の制約を確認した
- [ ] 想定読者をページごとに切り替える方針を確認した
- [ ] Wix を直接編集せず、まずコード側で実装する方針を確認した
- [ ] Tailwind CSS を使うが、ブランド色を優先する方針を確認した
- [ ] 日本語は敬体で書く方針を確認した

### 6-2. デザイン基盤
- [ ] `Noto Sans JP` と `Inter` を前提にしたタイポグラフィ設計を行った
- [ ] ブランド色を CSS 変数または Tailwind の拡張設定として定義した
- [ ] デフォルトの `blue-500` などを使っていないことを確認した
- [ ] セクションごとの余白、見出し階層、カードデザインを統一した
- [ ] `.reveal` と `IntersectionObserver` による表示アニメーションを組み込んだ

### 6-3. コンテンツ設計
- [ ] ページごとに想定読者を定義した
- [ ] 医局向けページ、一般向けページ、教育・採用ページの役割を整理した
- [ ] 専門用語に必要な補足説明を入れる方針を決めた
- [ ] 各ページの文章トーンを敬体で統一した
- [ ] 必要に応じて、読者層に合わせて説明密度を調整した

### 6-4. データ連携
- [ ] GAS から取得する JSON の項目を定義した
- [ ] スタッフ一覧、お知らせ、更新情報などのデータ構造を整理した
- [ ] フロントエンドで JSON を fetch して DOM を生成する実装にした
- [ ] ハードコードが残っていないことを確認した
- [ ] エラー時の代替表示を用意した

### 6-5. 埋め込み・公開方式
- [ ] 単体で動作する HTML ページとして成立している
- [ ] Wix の iframe 内でも崩れないレスポンシブ構造になっている
- [ ] 画像、フォーム、外部サービスの埋め込み先 URL を整理した
- [ ] Cloudflare Pages で公開できる構成になっている
- [ ] 独自ドメイン移行時の DNS 切替方針を確認した

### 6-6. セキュリティ
- [ ] API キーをフロントエンドに置かない設計にした
- [ ] Gemini API などの秘密情報はサーバーレス経由にした
- [ ] 外部 API の CORS と公開範囲を確認した
- [ ] 公開してよい情報と非公開情報を分けた

### 6-7. 実装後の確認
- [ ] PC とスマホの両方で表示確認をした
- [ ] iframe 内で横スクロールや崩れがないことを確認した
- [ ] 見出し、本文、リンク、ボタンの視認性を確認した
- [ ] 各ページの想定読者に対して説明が過不足ないか確認した
- [ ] 公開前に文言・リンク・埋め込み表示を最終確認した

### 6-8. 完了条件
- [ ] Cloudflare Pages で本番同等のページが表示できる
- [ ] GAS 連携が実データで動作する
- [ ] Wix 埋め込みでレイアウトが成立する
- [ ] 主要ページのテキストとデザインがブランド制約に沿っている
- [ ] 次の移行判断に必要な情報が揃っている

---
---
**関連リポジトリ:**
- [ent-landing-page (Master Repo)](file:///C:/Users/yert1/Documents/agy/50_SNS-WebSite/ent-landing-page)
- [ent-landing-page_codex (Codex Sandbox)](file:///C:/Users/yert1/Documents/agy/50_SNS-WebSite/ent-landing-page_codex)
- [[Wix-PDF-Library]]
- [[gas-mail_to_wix_instagram]]
- [[Gitプロジェクト一覧]]
